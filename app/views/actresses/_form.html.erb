<%= form_with(model: actress, local: true) do |form| %>
  <% if actress.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(actress.errors.count, "error") %> prohibited this actress from being saved:</h2>

      <ul>
      <% actress.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.text_field :image %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
